import os\nimport pandas as pd\n\ndef rename_files_from_excel(directory, excel_path):\n    # 读取 Excel 文件\n    df = pd.read_excel(excel_path)\n    \n    # 确保 Excel 文件中包含 'old_name' 和 'new_name' 列\n    if 'old_name' not in df.columns or 'new_name' not in df.columns:\n        print("Excel 文件中必须包含 'old_name' 和 'new_name' 列。")\n        return\n    \n    # 遍历 Excel 文件中的每一行\n    for index, row in df.iterrows():\n        old_name = row['old_name']\n        new_name = row['new_name']\n        \n        # 构建完整的文件路径\n        old_file_path = os.path.join(directory, old_name)\n        new_file_path = os.path.join(directory, new_name)\n        \n        # 检查文件是否存在\n        if os.path.exists(old_file_path):\n            # 重命名文件\n            os.rename(old_file_path, new_file_path)\n            print(f"文件 {old_name} 已重命名为 {new_name}")\n        else:\n            print(f"文件 {old_name} 不存在，跳过。")\n\n# 使用示例\ndirectory = 'path/to/your/files'  # 文件所在的目录\nexcel_path = 'path/to/your/excel/file.xlsx'  # Excel 文件的路径\nrename_files_from_excel(directory, excel_path)